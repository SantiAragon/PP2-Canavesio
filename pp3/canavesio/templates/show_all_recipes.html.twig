<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>All Recipes</title>
</head>
<body>
    <h1>All Recipes</h1>

    <h2>Machine Recipes</h2>
    <ul>
        {% for recipe in machine_recipes %}
            <li>
                <strong>{{ recipe.name }}</strong><br>
                Parts: 
                {% if recipe.parts is not empty %}
                    {% for part in recipe.parts %}
                        {{ part.name }}{% if not loop.last %}, {% endif %}
                    {% endfor %}
                {% else %}
                    No parts
                {% endif %}
                <br>
                Products: 
                {% if recipe.products is not empty %}
                    {% for product in recipe.products %}
                        {{ product.name }}{% if not loop.last %}, {% endif %}
                    {% endfor %}
                {% else %}
                    No products
                {% endif %}
                <br>
                <a href="{{ path('edit_recipe_machine', {'id': recipe.id}) }}">Edit</a>
            </li>
        {% else %}
            <li>No machine recipes found</li>
        {% endfor %}
    </ul>

    <h2>Product Recipes</h2>
    <ul>
        {% for recipe in product_recipes %}
            <li>
                <strong>{{ recipe.name }}</strong><br>
                Parts: 
                {% if recipe.parts is not empty %}
                    {% for part in recipe.parts %}
                        {{ part.name }}{% if not loop.last %}, {% endif %}
                    {% endfor %}
                {% else %}
                    No parts
                {% endif %}
                <br>
                
            </li>
        {% else %}
            <li>No product recipes found</li>
        {% endfor %}
    </ul>
</body>
</html>
